<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fetch</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous"> 
</head>

<body>
    <h2 class="head">Result</h2> <br>
    <div class="container">
         <h5>Data:</h5>

         <% userList.forEach((user) => {

            %> <p class="space">"Username": <a href="/users/<%= user.login %>"><%= user.login %></a></p>
            <p> "User-type": <%= user.type %></p> 
            <p>"User-avatar":
                &nbsp;
                <img class="pic" src="<%= user.avatar_url %>">
            </p>
            <p>
                <a href="<%= user.organizations_url %>">"Organizations"</a>
            </p>
            <p>
                <a href="<%= user.repos_url %>">"Repositories"</a>
            </p> <%

         }) %>
            
            
    </div>
         
    <div class="container">
        <div class="col-md-12 text-center">
            <a href="/" type="button" class="btn btn-primary">Go Back</a>
        </div>    
    </div>
    
</body>
</html>